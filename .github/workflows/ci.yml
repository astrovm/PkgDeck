name: CI
on:
  pull_request:
  push:
    tags: ['v*']
  workflow_dispatch:
    inputs:
      homebrew_only:
        description: Validate a published Homebrew formula on Linux and macOS only
        type: boolean
        default: false

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # PR and ordinary manual runs validate the full matrix. Formula updates use
  # a targeted manual run; tags reuse the PR run after release-gate verifies it.
  test-terminal:
    name: Test / Terminal (${{ matrix.arch }})
    if: github.event_name != 'push' && !inputs.homebrew_only
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-26.04
            arch: x86_64
          - runner: ubuntu-26.04-arm
            arch: aarch64
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install terminal dependencies
        run: sudo apt-get update && sudo apt-get install -y libapt-pkg-dev jq
      - name: Set up Rust
        run: rustup show
      - name: Cache Rust build outputs
        uses: ./.github/actions/rust-cache
        with:
          arch: ${{ matrix.arch }}
          scope: terminal
      - name: Run terminal checks
        run: scripts/verify.sh fast

  test-vm:
    name: Test / VM (x86_64)
    if: github.event_name != 'push' && !inputs.homebrew_only
    runs-on: ubuntu-26.04
    timeout-minutes: 35
    env:
      PKGDECK_STAGE_TIMEOUT: 30m
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install VM dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y qemu-system-x86 qemu-utils genisoimage
      - name: Restore terminal build outputs
        uses: ./.github/actions/rust-cache
        with:
          arch: x86_64
          scope: terminal
          save: 'false'
      - name: Cache prepared VM
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: build/host-vm
          key: ubuntu-host-vm-26.04-20260823-${{ hashFiles('scripts/vm/prepare.sh') }}
      - name: Run VM lifecycle and authorization tests
        run: scripts/verify.sh vm
      - name: Upload VM logs
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: logs-vm-x86_64
          path: |
            build/host-vm/logs
            build/verification

  test-podman:
    name: Test / Podman (${{ matrix.arch }})
    if: github.event_name != 'push' && !inputs.homebrew_only
    timeout-minutes: 35
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-26.04
            arch: x86_64
          - runner: ubuntu-26.04-arm
            arch: aarch64
    runs-on: ${{ matrix.runner }}
    env:
      PKGDECK_STAGE_TIMEOUT: 30m
      PKGDECK_CONTAINER_CACHE: ${{ github.workspace }}/build/container-cache
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install Podman dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y podman
          podman info
      - name: Cache container build outputs
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            build/container-cache
            !build/container-cache/target/**/incremental
          key: podman-v1-${{ matrix.arch }}-${{ hashFiles('containers/development.Containerfile', 'scripts/setup-dev.sh', 'scripts/dev-env.sh', 'Cargo.toml', 'crates/*/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml') }}-${{ github.sha }}
          restore-keys: podman-v1-${{ matrix.arch }}-${{ hashFiles('containers/development.Containerfile', 'scripts/setup-dev.sh', 'scripts/dev-env.sh', 'Cargo.toml', 'crates/*/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml') }}-
      - name: Run container workspace tests
        # Native jobs own lint/coverage; packaging owns release builds.
        run: scripts/verify.sh full --only tests --engine podman
      - name: Run container CLI lifecycle tests
        run: scripts/verify.sh containers --engine podman
      - name: Upload Podman logs
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: logs-podman-${{ matrix.arch }}
          path: build/verification

  test-native-backends:
    name: Test / Backend / ${{ matrix.backend }} (x86_64)
    if: github.event_name != 'push' && !inputs.homebrew_only
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        backend: [dnf, pacman, zypper]
    runs-on: ubuntu-26.04
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install Podman dependencies
        run: sudo apt-get update && sudo apt-get install -y podman
      - name: Restore terminal build outputs
        uses: ./.github/actions/rust-cache
        with:
          arch: x86_64
          scope: terminal
          save: 'false'
      - name: Build CLI
        run: cargo build --locked -p pkd
      - name: Run backend lifecycle tests
        run: scripts/tests/native-manager.sh ${{ matrix.backend }} "$PWD/target/debug/pkd"

  test-snap:
    name: Test / Backend / snap (x86_64)
    if: github.event_name != 'push' && !inputs.homebrew_only
    timeout-minutes: 25
    runs-on: ubuntu-26.04
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install Snap dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y snapd
          sudo systemctl enable --now snapd.socket
          sudo snap wait system seed.loaded
      - name: Restore terminal build outputs
        uses: ./.github/actions/rust-cache
        with:
          arch: x86_64
          scope: terminal
          save: 'false'
      - name: Build CLI
        run: cargo build --locked -p pkd
      - name: Run backend lifecycle tests
        run: scripts/tests/snap-manager.sh "$PWD/target/debug/pkd"

  test-dev-backends:
    name: Test / Backend / ${{ matrix.backend }} (x86_64)
    if: github.event_name != 'push' && !inputs.homebrew_only
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        backend: [cargo, npm, pnpm, bun, pip, pipx, uv, composer, gem]
    runs-on: ubuntu-26.04
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Restore terminal build outputs
        uses: ./.github/actions/rust-cache
        with:
          arch: x86_64
          scope: terminal
          save: 'false'
      - name: Build CLI
        run: cargo build --locked -p pkd
      - name: Run backend lifecycle tests
        run: scripts/tests/dev-manager.sh ${{ matrix.backend }} "$PWD/target/debug/pkd"

  lint-workspace:
    name: Lint / Workspace (${{ matrix.arch }})
    if: github.event_name != 'push' && !inputs.homebrew_only
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-26.04
            arch: x86_64
          - runner: ubuntu-26.04-arm
            arch: aarch64
    runs-on: ${{ matrix.runner }}
    env:
      QT_QPA_PLATFORM: offscreen
      QT_QUICK_BACKEND: software
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Cache Rust build outputs
        uses: ./.github/actions/rust-cache
        with:
          arch: ${{ matrix.arch }}
          scope: lint
      - name: Set up desktop dependencies
        uses: ./.github/actions/setup-desktop
      - name: Run workspace lint
        run: scripts/verify.sh full --only lint

  coverage-workspace:
    name: Coverage / Workspace (x86_64)
    if: github.event_name != 'push' && !inputs.homebrew_only
    timeout-minutes: 20
    runs-on: ubuntu-26.04
    env:
      QT_QPA_PLATFORM: offscreen
      QT_QUICK_BACKEND: software
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Cache Rust build outputs
        uses: ./.github/actions/rust-cache
        with:
          arch: x86_64
          scope: coverage
      - name: Set up desktop dependencies
        uses: ./.github/actions/setup-desktop
        with:
          coverage: 'true'
      - name: Run workspace coverage
        run: scripts/verify.sh full --only coverage
      - name: Upload coverage report
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: coverage-workspace-x86_64
          path: coverage/lcov.info
          if-no-files-found: error

  test-workspace:
    name: Test / Workspace (aarch64)
    if: github.event_name != 'push' && !inputs.homebrew_only
    timeout-minutes: 20
    runs-on: ubuntu-26.04-arm
    env:
      QT_QPA_PLATFORM: offscreen
      QT_QUICK_BACKEND: software
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Cache Rust build outputs
        uses: ./.github/actions/rust-cache
        with:
          arch: aarch64
          scope: tests
      - name: Set up desktop dependencies
        uses: ./.github/actions/setup-desktop
      - name: Run workspace tests
        run: scripts/verify.sh full --only tests

  package-homebrew:
    name: Package / Homebrew (${{ matrix.runner }})
    if: github.event_name != 'push'
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        runner: [ubuntu-26.04, macos-26]
    env:
      HOMEBREW_NO_AUTO_UPDATE: '1'
      HOMEBREW_NO_INSTALL_CLEANUP: '1'
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Configure Homebrew
        shell: bash
        run: |
          if [[ "$RUNNER_OS" == Linux ]]; then
            echo /home/linuxbrew/.linuxbrew/bin >> "$GITHUB_PATH"
          fi
      - name: Prepare formula for this checkout
        if: '!inputs.homebrew_only'
        shell: bash
        run: |
          archive="$RUNNER_TEMP/pkgdeck-source.tar.gz"
          git archive --format=tar.gz --output="$archive" HEAD
          brew tap-new --no-git astrovm/pkgdeck
          recipe="$(brew --repository astrovm/pkgdeck)/Formula/pkgdeck.rb"
          brew ruby -r digest -e '
            source = ARGV.fetch(0)
            recipe = File.read("Formula/pkgdeck.rb")
            version = File.read("Cargo.toml")[/^version = "([^"]+)"$/, 1]
            stable = "url #{("file://" + source).inspect}\n  version #{version.inspect}\n  sha256 #{Digest::SHA256.file(source).hexdigest.inspect}"
            recipe.sub!(/^  head .*\n/, "")
            recipe.sub!(/^  license /, "  #{stable}\n  license ")
            File.write(ARGV.fetch(1), recipe)
          ' "$archive" "$recipe"
      - name: Use published formula from this checkout
        if: inputs.homebrew_only
        shell: bash
        run: |
          brew tap-new --no-git astrovm/pkgdeck
          cp Formula/pkgdeck.rb "$(brew --repository astrovm/pkgdeck)/Formula/pkgdeck.rb"
      - name: Check formula style
        run: brew style astrovm/pkgdeck/pkgdeck
      - name: Build package
        run: brew install --build-from-source --verbose astrovm/pkgdeck/pkgdeck
      - name: Test installed commands and GUI startup
        run: brew test astrovm/pkgdeck/pkgdeck
      - name: Check CLI has no Qt linkage
        shell: bash
        run: |
          if [[ "$RUNNER_OS" == macOS ]]; then
            links=$(otool -L "$(brew --prefix astrovm/pkgdeck/pkgdeck)/bin/pkd")
          else
            links=$(ldd "$(brew --prefix astrovm/pkgdeck/pkgdeck)/bin/pkd")
          fi
          if printf '%s\n' "$links" | grep -qi Qt; then
            echo 'CLI unexpectedly links Qt' >&2
            exit 1
          fi

  package:
    name: Package / Linux (${{ matrix.arch }})
    if: '!inputs.homebrew_only'
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-26.04
            arch: x86_64
          - runner: ubuntu-26.04-arm
            arch: aarch64
    runs-on: ${{ matrix.runner }}
    env:
      QT_QPA_PLATFORM: offscreen
      QT_QUICK_BACKEND: software
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Cache Rust build outputs
        uses: ./.github/actions/rust-cache
        with:
          arch: ${{ matrix.arch }}
          scope: release
      - name: Set up desktop dependencies
        uses: ./.github/actions/setup-desktop
        with:
          packaging: 'true'
      - name: Build release binaries
        run: scripts/verify.sh full --only release
      - name: Test staged bundle
        run: |
          source scripts/dev-env.sh
          cargo build --locked -p pkgdeck-tools
          scripts/bundle.sh
          scripts/tests/media-cache.sh "$PWD/build/AppDir/usr"
          test -x build/AppDir/usr/lib/pkgdeck/appimageupdatetool.AppImage
          desktop-file-validate assets/io.github.astrovm.PkgDeck.desktop
          unset LD_LIBRARY_PATH QML_IMPORT_PATH
          scripts/xtask.sh gui build/AppDir/AppRun
          build/AppDir/AppRun --cli </dev/null | grep -F 'Usage: pkd'
          build/AppDir/AppRun --cli --version
          build/AppDir/AppRun --cli doctor
      - name: Run packaged GUI lifecycle tests
        run: PKGDECK_FRONTEND=gui PKGDECK_BINARY_DIR="$PWD/build/AppDir/usr/bin" scripts/container.sh lifecycle
      - name: Build and test AppImage
        env:
          APPIMAGETOOL: ${{ github.workspace }}/build/appimagetool
        run: |
          curl -fL --retry 3 'https://github.com/AppImage/appimagetool/releases/download/1.9.1/appimagetool-${{ matrix.arch }}.AppImage' -o "$APPIMAGETOOL"
          chmod +x "$APPIMAGETOOL"
          scripts/package.sh appimage
          'build/artifacts/PkgDeck-${{ matrix.arch }}.AppImage' --appimage-extract 'usr/lib/pkgdeck/appimageupdatetool.AppImage' >/dev/null
          test -x 'squashfs-root/usr/lib/pkgdeck/appimageupdatetool.AppImage'
          rm -rf squashfs-root
          unset LD_LIBRARY_PATH QML_IMPORT_PATH
          export APPIMAGE_EXTRACT_AND_RUN=1
          scripts/xtask.sh gui 'build/artifacts/PkgDeck-${{ matrix.arch }}.AppImage'
          'build/artifacts/PkgDeck-${{ matrix.arch }}.AppImage' --cli </dev/null | grep -F 'Usage: pkd'
          'build/artifacts/PkgDeck-${{ matrix.arch }}.AppImage' --cli doctor
          appimage=$PWD/build/artifacts/PkgDeck-${{ matrix.arch }}.AppImage
          scripts/tests/host-package.sh direct "$appimage" --cli
          data=$PWD/build/appimage-backend-data
          XDG_DATA_HOME="$data" "$appimage" --cli --json --yes --from appimage search "$appimage" | jq -e '.exit_code == 0'
          XDG_DATA_HOME="$data" "$appimage" --cli --json --yes --from appimage install "$appimage" | jq -e '.exit_code == 0'
          imported=$(XDG_DATA_HOME="$data" "$appimage" --cli --json --from appimage list | jq -r '.data.packages[0].id.name')
          XDG_DATA_HOME="$data" "$appimage" --cli --json --yes --from appimage remove "$imported" | jq -e '.exit_code == 0'
      - name: Build and test Flatpak
        run: |
          sudo flatpak remote-add --system --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
          sudo flatpak install --system -y --noninteractive flathub org.kde.Platform//6.11 org.kde.Sdk//6.11 org.freedesktop.Sdk.Extension.rust-stable//25.08
          scripts/package.sh flatpak
          sudo flatpak install --system -y --noninteractive 'build/artifacts/PkgDeck-${{ matrix.arch }}.flatpak'
          scripts/xtask.sh gui flatpak run --env=QT_QPA_PLATFORM=offscreen --env=QT_QUICK_BACKEND=software io.github.astrovm.PkgDeck
          flatpak run --command=pkd io.github.astrovm.PkgDeck </dev/null | grep -F 'Usage: pkd'
          scripts/tests/flatpak-package.sh
      - name: Build and test Snap
        run: |
          scripts/package.sh snap
          unsquashfs -d build/snap-check build/artifacts/*.snap
          test -x build/snap-check/usr/bin/pkgdeck
          test -x build/snap-check/usr/bin/pkd
          unset LD_LIBRARY_PATH QML_IMPORT_PATH
          scripts/xtask.sh gui build/snap-check/AppRun
          build/snap-check/usr/bin/pkd </dev/null | grep -F 'Usage: pkd'
          sudo systemctl enable --now snapd.socket
          sudo snap wait system seed.loaded
          sudo snap install --dangerous --classic build/artifacts/*.snap
          snap run pkgdeck.pkd --version
          snap run pkgdeck.pkd doctor
          scripts/tests/host-package.sh direct snap run pkgdeck.pkd
      - name: Upload packages
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: packages-linux-${{ matrix.arch }}
          path: build/artifacts/*
          if-no-files-found: error

  release-gate:
    name: Release / Verify reviewed commit
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-26.04
    permissions:
      actions: read
      contents: read
      pull-requests: read
    steps:
      - name: Check out tagged source and main history
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
      - name: Verify tag matches a merged, CI-tested pull request
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$GITHUB_SHA"
          git fetch --no-tags origin main
          if ! git merge-base --is-ancestor "$GITHUB_SHA" origin/main; then
            echo '::error::Release tag must point to a commit on main.'
            exit 1
          fi

          pr=$(gh api "repos/$GITHUB_REPOSITORY/commits/$GITHUB_SHA/pulls" |
            jq -r --arg sha "$GITHUB_SHA" \
              '[.[] | select(.merged_at != null and .base.ref == "main" and .merge_commit_sha == $sha)] | first | if . == null then empty else [.number, .head.sha, .head.repo.full_name] | @tsv end')
          if [[ -z "$pr" ]]; then
            echo '::error::Release tag must point to a pull request merge commit on main.'
            exit 1
          fi
          IFS=$'\t' read -r pr_number head_sha head_repo <<< "$pr"
          test -n "$head_sha"
          test -n "$head_repo"

          head_tree=$(gh api "repos/$GITHUB_REPOSITORY/git/commits/$head_sha" --jq '.tree.sha')
          if [[ "$head_tree" != "$(git rev-parse 'HEAD^{tree}')" ]]; then
            echo '::error::Tagged tree differs from the reviewed pull request head.'
            exit 1
          fi

          run=$(gh api --method GET "repos/$GITHUB_REPOSITORY/actions/workflows/ci.yml/runs" \
            -f event=pull_request -f "head_sha=$head_sha" -f per_page=100 |
            jq -r --arg sha "$head_sha" --arg repo "$head_repo" \
              '[.workflow_runs[] | select(.event == "pull_request" and .head_sha == $sha and .head_repository.full_name == $repo and .status == "completed" and .conclusion == "success" and .path == ".github/workflows/ci.yml")] | first | .html_url // empty')
          if [[ -z "$run" ]]; then
            echo "::error::PR #$pr_number has no successful CI run for its merged head commit."
            exit 1
          fi
          echo "Release tag matches PR #$pr_number and successful CI run $run"

  release:
    name: Publish / Release
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [release-gate, package]
    runs-on: ubuntu-26.04
    permissions:
      contents: write
      id-token: write
      attestations: write
    steps:
      - name: Check out tagged source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Download Linux packages
        uses: actions/download-artifact@70fc10c6e5e1ce46ad2ea6f2b72d43f7d47b13c3 # v8.0.0
        with:
          pattern: packages-linux-*
          path: dist
          merge-multiple: true
      - name: Verify version and generate update metadata
        shell: bash
        run: |
          version=${GITHUB_REF_NAME#v}
          manifest_version=$(sed -n 's/^version = "\([^"]*\)"/\1/p' Cargo.toml | head -n1)
          test "$version" = "$manifest_version"
          sudo apt-get update
          sudo apt-get install -y zsync
          for image in dist/*.AppImage; do zsyncmake -u "$(basename "$image")" -o "$image.zsync" "$image"; done
          scripts/flatpak-release-manifest.sh "$GITHUB_REF_NAME" dist/io.github.astrovm.PkgDeck.yml
          cp packaging/flatpak/cargo-sources.json dist/cargo-sources.json
          (cd dist && sha256sum -- * > SHA256SUMS)
      - name: Attest release packages
        uses: actions/attest-build-provenance@e8998f949152b193b063cb0ec769d69d929409be # v2.4.0
        with:
          subject-path: dist/*
      - name: Publish GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release create "$GITHUB_REF_NAME" dist/* --verify-tag --generate-notes --title "PkgDeck $GITHUB_REF_NAME"
  publish:
    name: Publish
    if: startsWith(github.ref, 'refs/tags/v')
    needs: release
    permissions:
      actions: write
      contents: write
      pull-requests: write
    uses: ./.github/workflows/publish.yml
    with:
      tag: ${{ github.ref_name }}
    secrets: inherit
